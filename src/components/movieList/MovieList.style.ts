import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingVertical: 20,
  },
  title: {
    paddingHorizontal: 20,
    paddingBottom: 15,
    fontSize: 18,
    fontWeight: 'bold',
  },
});
