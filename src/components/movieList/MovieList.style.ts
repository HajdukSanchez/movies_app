import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingTop: 5,
    paddingBottom: 10,
  },
  title: {
    paddingHorizontal: 20,
    paddingBottom: 15,
    fontSize: 18,
    fontWeight: 'bold',
  },
});
